---
global:
  storageClass: "longhorn" # Change this to your prefered storage provider.  kubectl get storageclasses
production: true
proxy: edge
proxyAddressForwarding: true

auth:
  adminUser: admin
  adminPassword: "test123" # dont actually use this
#tls:
#  enabled: true
#  autoGenerated: true
ingress:
  enabled: true
  #servicePort: https
  ingressClassName: "nginx"
  hostname: "keycloak.home.local"
  tls: true
  pathType: Prefix
  annotations:
    #cert-manager.io/cluster-issuer: selfsigned-cluster-issuer # this is the name of my cluster issuer instance
    #nginx.ingress.kubernetes.io/ssl-redirect: "true"
    external-dns.alpha.kubernetes.io/hostname: "keycloak.home.local"
    cert-manager.io/issuer-group: certmanager.step.sm
    cert-manager.io/issuer-kind: StepClusterIssuer
    cert-manager.io/issuer: step-cluster-issuer
